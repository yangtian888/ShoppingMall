<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shoppingmall.dao.mapper.VipMapper">
 	<update id="saveinfo">
 		update shoppinguser set realname=#{realname},city=#{city},sex=#{sex},
 		email=#{email},	idcard=#{idcard},synopsis=#{sysopsis} where id = #{id}
 	</update>
</mapper> 